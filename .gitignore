node_modules/
.env

# Measurement output, regenerated by npm run bench / loadtest.
benchmark-results.json
loadtest-results.json
.test-cache.db

# Local caches: the DHT routing table, per-infohash swarm health, and the
# crawler's index. All rebuildable, none meaningful on another machine.
.peer-cache.json
.peer-cache.db
.peer-cache.db-wal
.peer-cache.db-shm
.dht-index.db
.dht-index.db-wal
.dht-index.db-shm

worker/.wrangler/
